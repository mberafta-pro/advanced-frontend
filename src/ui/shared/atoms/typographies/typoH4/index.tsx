import { Typography } from '@mui/material';
import React from 'react';

export const TypoH4: React.FC = ({ children }) => (
  <Typography variant="h4" color="primary">
    {children}
  </Typography>
);
