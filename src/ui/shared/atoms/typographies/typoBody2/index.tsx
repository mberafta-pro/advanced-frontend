import { Typography } from '@mui/material';
import React from 'react';

export const TypoBody2: React.FC = ({ children }) => (
  <Typography variant="body2" color="primary">
    {children}
  </Typography>
);
